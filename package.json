{
  "name": "quiz-ai-root",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  }
}